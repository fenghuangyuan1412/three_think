# agent.md —— three_think 项目纲领

> 本文件是本仓库的**结构文件**，任何 agent 或人在**写代码之前必须先读**。
> 本文件写的是**约束**，不是建议。违反约束的改动一律视为错误，必须回退。
> 提交规则见 [`git.md`](./git.md)；Three.js 具体做法见 [`.dsh/skills/threejs-game-dev/SKILL.md`](./.dsh/skills/threejs-game-dev/SKILL.md)。

---

## 0. 项目定位

`three_think` 是一个**融合多种棋类玩法的 3D 桌游**。

它不是一个单独的棋类游戏，而是一张**能承载多种棋类的桌子**：
棋盘、棋子、回合、胜负判定是通用底座，各棋类玩法以可插拔的规则模块形式挂上去。

| 项 | 内容 | 状态 |
| --- | --- | --- |
| 玩法 | 融合多种桌游玩法的桌游；第一版是**马尼拉式竞标**模块 | 已定 |
| 技术栈 | Three.js + TypeScript + Vite + Vitest | **已定** |
| 运行形态 | 浏览器单页应用 | **已定** |
| 目标平台 | 桌面浏览器优先，兼容移动浏览器 | **已定** |
| 联机 | 第一版本地热座；**架构必须预留联机接口**（见 §5） | **已定** |
| 美术 | 程序化几何体 + Canvas 贴图，**不引入外部美术资源** | **已定** |
| 后续棋类模块 | 未定 | 待确认 |

**"待确认"不等于可以自由发挥。** 涉及上表未定项的实现，先问仓库主人，不要替他把玩法定死。

### 当前进度

**完整游戏流程已实现并验证通过**：竞标港务长 → 买股份 → 装货 → 放船 →
放置小弟 ⇄ 掷骰推船（含海盗登船 / 劫掠、领航员）→ 海盗决定去向 → 利润分配与保险理赔 →
货物涨价 → 下一段航程 → 任一货价达 30 元时游戏结束。

尚未实现：原版的 **20 张行动牌**；海盗登船目前是**自动**的（原版逐个询问）。

- 流程细节、简化项、待核对数值、实测数据：[`docs/game-flow.md`](./docs/game-flow.md)
- 第一版（仅竞标）的规则对照与踩坑记录：[`docs/design-v1.md`](./docs/design-v1.md)

---

## 1. 动代码之前的固定动作

按顺序做，不要跳：

1. 读本文件（`agent.md`）。
2. 读 `.dsh/skills/threejs-game-dev/SKILL.md`，涉及 3D/交互/资源的改动必读。
3. **改玩法、状态机或结算逻辑时，先读 §13 游戏流程**，以及 `docs/game-flow.md`。
4. 确认这次改动落在 §3 的哪一层，以及它是否触碰 §2 的红线。
5. 改动完成后，按 §7 验证，按 `git.md` §3 自检，最后才提交。

---

## 2. 🚫 绝对禁止（红线）

1. **禁止把玩法规则写进渲染层。** 见 §3，这是本项目最重要的一条。
2. **禁止引入许可不明的美术资源。** 只接 CC0 或授权明确的资源；每个资源必须登记来源与许可（§8）。不确定就换资源。
3. **禁止提交密钥、令牌、`.env`、私钥。** 本仓库是公开仓库（见 `git.md` §6）。
4. **禁止提交 `node_modules/`、`dist/`、构建产物、日志。** 见 `.gitignore`。
5. **禁止把本机绝对路径、内网 IP、机器名写进代码或配置。** 这类信息如需记录，放 `docs/` 并标注"本机环境"。
6. **禁止 `git add -f` 绕过 `.gitignore`。**
7. **禁止 force push 到 `main`**，禁止自作主张 merge 到 `main`。必须由仓库主人确认。
8. **禁止在逻辑代码里写魔术数字。** 棋盘尺寸、回合上限、计时、分值一律进 `src/config/`。
9. **禁止用未固定种子的随机数驱动对局。** 洗牌、掷骰必须走带种子的 PRNG（§5）。

---

## 3. 架构分层（目录约定）

```
src/
├── core/      纯规则：棋盘状态、走子合法性、回合机、胜负判定
├── render/    Three.js：场景、网格、材质、光照、动画、相机
├── input/     指针 / 键盘 → 意图对象
├── ui/        DOM 面板、按钮、结算层、设置
├── config/    棋盘尺寸、规则参数、性能预算等常量
└── assets/    资源加载与登记
assets/        运行时美术资源（GLB / KTX2 / WebP）
tests/         测试
docs/          设计文档、资源登记
```

**依赖方向是单向的**：`ui → input → render → core`。`core/` 不 import 任何上层，尤其不 import `three`。

三条可执行的判据：

1. 删掉整个 `src/render/`，`src/core/` 的单元测试仍然全绿。
2. 任一规则可以用一个不创建渲染器、不加载资源的纯函数测试。
3. 渲染层里搜不到 `isValidMove`、`canPlace`、`checkWin` 这类判定调用——合法与否由 `core/` 算好后传进来。

**输入统一为意图对象**，例如 `{ type: 'move', from, to }`。规则层校验失败返回原因，`ui/` 负责提示；`render/` 不参与判定。

---

## 4. 棋类玩法模块的设计约束

这是"融合多种棋类"能成立的唯一前提。

1. **一个棋类玩法 = 一个模块**，实现同一套规则接口（棋盘表示、走子生成、合法性校验、胜负判定、可选的 AI 钩子），注册进规则注册表。
2. **接口先定，玩法后加。** 新增第二种棋类时，如果需要改接口才能塞进去，说明接口错了——先改接口，再改玩法。
3. **规则模块之间零耦合。** A 棋类不得 import B 棋类；共用的东西上提到 `core/`。
4. **非法走法必须被显式拒绝并给出原因**，不能静默忽略。每条规则至少配一条"非法输入被拒"的测试。
5. **融合玩法要写清交互规则**：两种棋类的规则同时生效时，谁优先、冲突如何裁决。这一条必须落在 `core/` 的测试里，不能只写在文档里。

---

## 5. 对局状态、回放与悔棋

- **状态机显式化**：`idle → 选子 → 预演 → 确认 → 结算 → 换手`。禁止用一堆布尔量拼凑状态。
- **状态不可变**：每次走子产生新快照。悔棋 = 弹栈，不需要写反向运算。
- **必须有动作日志**（action log）。没有日志，玩家报的 Bug 无法复现。
- **随机数带种子**：种子写进对局记录。复盘、联机同步、Bug 复现都靠它。
- **回合制逻辑不依赖时间**，不得依赖帧率或 `deltaTime`。

---

## 6. 性能预算

先定预算，再优化。超出下表即为缺陷，不是"以后再优化"。

| 项 | 桌面 | 移动 |
| --- | --- | --- |
| 帧率 | 60 fps | ≥ 30 fps |
| Draw call | < 150 | < 60 |
| 三角形 | < 300k | < 100k |
| 首屏可交互 | < 2 s | < 4 s |
| 纹理单张 | ≤ 2048² | ≤ 1024² |

其他硬性要求：

- 资源卸载必须显式 `dispose()`（geometry / material / texture），并从场景 `remove()`。切换场景后显存必须回落。
- 同一模型多实例用 `InstancedMesh` 或共享 geometry + material。
- 动画用 `renderer.setAnimationLoop` / `requestAnimationFrame`，禁用 `setInterval`。
- 事件监听（`addEventListener`、`ResizeObserver`）必须成对摘除。

---

## 7. 测试与验证

| 改动类型 | 必须提供的证据 |
| --- | --- |
| `core/` 规则 | 单元测试通过，含"非法走法被拒"用例 |
| `render/` `input/` `ui/` | 真实浏览器里手动走通受影响流程，控制台无报错 |
| 性能相关 | 改动前后的 `renderer.info.render.calls / triangles` 数值 |
| 美术资源 | 资源体积、加载耗时、许可来源三项齐全 |
| 任何改动 | 类型检查、构建通过 |

**没有证据不许说"已完成"。** 没验证的事情写"未验证"，不要写"应该可以"。

---

## 8. 美术资源

### 8.1 可用来源（白名单）

只从下表选。表外的来源，先确认许可再登记。

| 来源 | 内容 | 许可 | 链接 |
| --- | --- | --- | --- |
| Kenney | 低多边形模型、UI、音效 | CC0 | <https://kenney.nl/assets> |
| Quaternius | 低多边形角色 / 道具 / 自然 | CC0 | <https://quaternius.com/> |
| Poly Haven | HDRI、PBR 贴图、模型 | CC0 | <https://polyhaven.com/> |
| ambientCG | PBR 材质贴图 | CC0 | <https://ambientcg.com/> |
| Open Source 3D Assets | GLB / glTF 模型合集 | CC0 | <https://www.opensource3dassets.com/> |
| KayKit（Kay Lousberg） | 角色、场景、动画包 | CC0（逐包确认） | <https://kaylousberg.itch.io/> |
| Poly Pizza | 模型聚合（多来源） | 逐项确认，多为 CC0 / CC-BY | <https://poly.pizza/> |
| itch.io 免费资源 | 杂项 | **逐项确认**，必须署名时署名 | <https://itch.io/game-assets/free> |

优先 CC0：CC0 不要求署名，能省掉后续所有版权麻烦。CC-BY 可用，但必须在 §8.2 登记署名信息并在游戏内"关于"页展示。

### 8.2 资源登记（强制）

**新增任何美术资源，必须在本节表格追加一行，并在同一次提交里完成。** 没登记的资禁止合入。

| 资源 | 来源 URL | 作者 | 许可 | 用途 | 登记日期 |
| --- | --- | --- | --- | --- | --- |
| （示例，待替换）board-tile.glb | <https://kenney.nl/assets/xxx> | Kenney | CC0 | 棋盘格 | 待填 |

### 8.3 格式与体积

- 格式优先级：**GLB > glTF + bin**。GLB 单文件、少一次请求。
- 几何压缩用 **Draco** 或 meshopt，纹理优先 **KTX2（Basis）** 或 **WebP**。
- 压缩后必须实测首屏时间，不能只看文件大小。
- Blender / Substance 源文件（`*.blend`、`*.spp`、`*.psd`）不入库，见 `.gitignore`。

---

## 9. 代码规范

- TypeScript `strict` 打开，不用 `any` 兜底；确实需要时写 `unknown` + 收窄。
- 命名：文件 `kebab-case.ts`，类型 `PascalCase`，函数与变量 `camelCase`，常量 `UPPER_SNAKE_CASE`。
- 一个文件只做一件事。**超过 300 行先想是不是该拆**，不是硬性上限，但是明确的信号。
- 注释写**为什么**，不写"这行在干什么"。玩法规则参数必须注明出处（哪本规则书 / 哪次讨论结论）。
- 提交前删掉调试用的 `console.log`。

---

## 10. 分支与协作

- `main` 是**稳定可发布态**，任何人不得在 `main` 上堆未验证的内容。
- 功能走分支：`feat/<主题>`、`fix/<主题>`、`rules/<棋类>`、`assets/<资源包>`。详见 `git.md` §2。
- 多个分支互不干扰，合入串行，顺序由仓库主人裁决。
- **分支冲突不要自作主张解决**，报告给仓库主人。

---

## 11. 待仓库主人拍板的开放项

以下问题会实质影响架构，落地前必须确认：

1. ~~技术栈、运行形态、目标平台、联机方式、美术方式~~ —— **已确认**，见 §0 表格。
2. **第一版之后还要融合哪些桌游玩法？** 决定 `core/` 的规则接口能否保持不变。新增玩法时若需要改接口，先改接口再改玩法（§4）。
3. **是否引入物理引擎？**（`Rapier` / `cannon-es`）回合制桌游通常不需要，落子动画用补间即可。
4. **是否需要 AI 对手？** 需要的话，搜索算法挂在 `core/`，仍不得依赖渲染。
5. **是否用 React / R3F 管 UI？** 会影响 `ui/` 的写法，但**不影响 §3 的分层结论**。
6. **原版棋盘上印刷的数值**（价格轨刻度、港口/修船场报酬、货仓利润）—— 见 [`docs/game-flow.md`](./docs/game-flow.md) §5.3，结算结果要忠实于原版就必须核实。

---

## 12. 待办

- ~~`README.md`~~ —— 已写（面向玩家，只介绍游戏，不搬运本纲领）。
- **海盗登船目前是自动的**，原版是每位海盗依序决定是否登船（`docs/game-flow.md` §5.2）。
- **原版 20 张行动牌未实现。**
- **模型与美术**由仓库主人后续处理，当前全部为程序化几何体。

---

## 13. 游戏流程（改玩法逻辑前必读）

完整细节见 [`docs/game-flow.md`](./docs/game-flow.md)。这里是必须记住的骨架。

### 13.1 一局的走向

```
建局（3–5 人）→ 每段航程【竞标港务长 → 买股份 → 装货 → 放船
  → 放置小弟 ⇄ 掷骰推船（含海盗、领航员）→ 海盗决定去向
  → 利润分配与保险理赔 → 抵达货物涨价】
  → 任一货价达 30 元则游戏结束，否则进入下一段航程
```

### 13.2 两个必须记住的结构

1. **步骤表是数据驱动的**（`core/voyage.ts` 的 `voyageSchedule`），不是散落的 if：
   3 人局 `P P M P M P pilot M`，4–5 人局 `P M P M P pilot M`。
   `P` 放置小弟、`M` 掷骰推船。**小弟总数与放置回合数刻意对齐**（3 人局 4/4、4–5 人局 3/3），
   所以每人每回合放一个，一程正好用完。改人数相关逻辑前先确认这条对齐关系没被破坏。
2. **状态推进只有 `applyIntent(state, intent)` 一个入口**（`core/game.ts`）。
   新增玩法动作 = 新增一个 Intent 分支，不要绕过它直接改 state。

### 13.3 阶段清单

`setup` → `auction` → `buy-share` → `load` → `launch` →
`placement` ⇄ `movement` → `pilot` → `pirate-destination` →
`payout` → `price-rise` → （`auction` 或 `game-over`）

`ui/phase-views.ts` 里每个阶段一个视图函数，加阶段时两处都要加。

### 13.4 容易写错、已被测试钉住的规则

| 规则 | 坑 |
| --- | --- |
| 三船起点**之和必须正好是 9**，各自在 0–5 | 只校验范围不校验和 |
| 货仓**必须从最低价的空位**开始填 | 做成自由选格 |
| 海盗船**第一个占位者才是船长** | 做成自由选两格 → 船长永远不会产生 |
| **越过**第 13 格才进港，**正好停在第 13 格**招来海盗 | 把 `>= 13` 当成进港 |
| 保险处费用为 0 | 若把保险处算进「最便宜的格位」，盲目旅客判定永不成立 |
| 被劫掠时**只有登船的海盗**分钱 | 靠 `Placement.fromPirate` 区分，不能只看格位 |
| 保险理赔：仲介者**先收利润再付理赔** | 顺序反了会导致错误地被迫贷款 |
| 竞标结算后要**同时**设置 `harborMaster` 与阶段 | 漏设会让后续阶段找不到港务长 |

### 13.5 数值可信度

`src/config/board-layout.ts` 的 `PRINTED_VALUES_PROVENANCE` 当前为 `'verified'`：
港口 / 修船场报酬、货仓费用与利润、价格轨刻度、海盗船与领航员费用、骰子颜色
**都已对照 Zoch Verlag 官方英文规则书核实**（来源与逐项出处在
[`docs/game-flow.md`](./docs/game-flow.md) §5.3）。

改动这些数值前必须先确认来源。改动后把出处一并更新到 §5.3，不要只改数字。

**两条仍未确定、但已按合理方式处理的**（改相关逻辑前先读 §5.3）：

1. 蓝 / 米黄 / 绿三颗骰对应哪种货，规则书没有明文（只有颜色体系推断）。
2. 港口 / 修船场每格印了几个小弟位，英文规则书表述自相矛盾；实物照每格只见 1 个圆圈，
   本实现按**每格 1 位**处理。

### 13.7 棋盘布局的三条硬约束

改 `src/render/coords.ts` / `board.ts` 时注意，这三条都是踩过的坑：

1. **相机对准的是航道中心线（x=0），不是包围盒中心。** 包围盒中心偏向右侧
   （价格轨与修船场都在右边），对准它会让透视把左侧航道画得比右侧低，看起来像长度不同。
2. **格位的 Z 必须沿「A 最远 → C 最近」递增。** 写成 `Z0 - index * PITCH` 会把 B、C
   排到棋盘外面（实测 B/C 落到了 `z = -14.1 / -15.8`，整格消失）。
3. **台面上的文字标签要离台面够高，且必须深字配浅底。** 标签与台面只差 0.025 时会发生
   z-fighting 导致部分标签被吞掉；深色字印在深色木台上则基本看不见 —— 港口与修船场的台面
   因此特意改成浅木色。

另外，货舱板块的数字**不能平贴在甲板上**（会被桅杆与帆挡住），要用悬浮牌挂在帆之上。

### 13.8 界面上的「收益」有两套，别混淆

- **棋盘上印的**是固定的印刷数值（`config/board-layout.ts`），不随对局变化。
- **侧栏「本航程收益一览」**是**当下估算**（`ui/earnings.ts`）：货仓按已站几个小弟算每人能分多少，
  港口 / 修船场按还差几艘船才算达成。它**不参与结算** —— 结算永远由 `core/payout.ts` 按实际结果算。
  格位按钮上的「付X → 得Y」来自 `core/placement.ts` 的 `previewSpot()`，同样只是给玩家决策用的。

---

## 附：本文件的效力

- 本文件与 `git.md` 冲突时，以更严格的一条为准。
- 需要修改本文件时，在提交信息里说明**改了哪条约束、为什么**，并等仓库主人确认。
